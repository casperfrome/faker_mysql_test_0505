"""
CasPerFect 模拟数据生成器 - 集中配置（示例）
复制此文件为 config.py 并填入实际值：
    cp config.example.py config.py
"""

# ============================================================
# MySQL 连接（凭证集中管理 — 不要把密码散落到其他文件）
# ============================================================
MYSQL = {
    "host": "127.0.0.1",
    "port": 3306,
    "user": "root",
    "password": "your_password_here",
    "charset": "utf8mb4",
}

DB_NAME = "CasPerFect_260505"

# ============================================================
# 时间范围
# ============================================================
DATE_START = "2025-06-04"
DATE_END   = "2026-05-05"

# ============================================================
# 业务规模
# ============================================================
NUM_STORES          = 50
NUM_PRODUCTS        = 90
TARGET_ORDERS       = 2_000_000        # 总订单目标(全期)
EMPLOYEES_PER_STORE = (25, 35)         # 每店员工区间

# 会员相关
MEMBER_BASE_COUNT   = 100_000          # 会员主表行数(含老会员)
MEMBER_ORDER_RATIO  = 0.25              # 会员订单占比
COUPON_USE_RATIO    = 0.12              # 券核销订单占比

# 渠道占比 (堂食/自取/美团/饿了么)
CHANNEL_RATIO = {
    "DINE_IN":   0.45,
    "TAKEOUT":   0.10,
    "MEITUAN":   0.30,
    "ELEME":     0.15,
}

# 支付方式占比
PAYMENT_RATIO = {
    "WECHAT":   0.45,
    "ALIPAY":   0.30,
    "MEMBER":   0.12,    # 会员储值
    "CARD":     0.08,
    "CASH":     0.05,
}

# ============================================================
# 写入参数
# ============================================================
BATCH_SIZE  = 5000
RANDOM_SEED = 20260505

# ============================================================
# 仿真特征开关 (调试时可以关掉以加速)
# ============================================================
ENABLE_HOLIDAY      = True   # 节假日效应
ENABLE_WEEKEND      = True   # 周末效应
ENABLE_SEASONAL     = True   # 季节性 (冷热饮)
ENABLE_LIFECYCLE    = True   # 新店爬坡/衰退期
